"""HTTP parsing package using httptools.

This subpackage provides a set of HTTP parsing APIs that is decoupled from
network IO by using the httptools library (Python bindings to llhttp, the
HTTP parser used in Node).

This package is internal; import public names from ``localstub``.

"""
